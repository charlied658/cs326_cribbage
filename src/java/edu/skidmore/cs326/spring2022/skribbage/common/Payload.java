package edu.skidmore.cs326.spring2022.skribbage.common;

/**
 * Marker interface (bad practice, will refactor into Annotation at some point)
 * for tagging data as a 'payload', allowing it to be passed through the event
 * system.
 */
public interface Payload {
}
